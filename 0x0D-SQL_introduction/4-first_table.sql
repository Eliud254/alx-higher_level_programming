-- Script that creates a table ,first_table in the current databas
-- Database to be passed as an argument of the mysql command
CREATE TABLE IF NOT EXISTS first_table
       (`id` int, `name` varchar(256))
